<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HostApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HostApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Cross-process WinUI widget host">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock FontSize="24" FontWeight="SemiBold" Text="Out-of-process WinUI 3 page" />
            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="WidgetApp.exe owns the Page; Windows embeds its HWND and routes input." />
        </StackPanel>

        <Border x:Name="WidgetSlot"
                Grid.Row="1"
                MinHeight="320"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <ProgressRing x:Name="ConnectionProgress" Width="16" Height="16" IsActive="True" />
            <TextBlock x:Name="ConnectionStatus"
                       VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="Starting WidgetApp.exe…" />
        </StackPanel>
    </Grid>
</Window>
