<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HostApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Cross-process WinUI widget host">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" Padding="24,24,24,0" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock FontSize="24"
                       FontWeight="SemiBold"
                       Text="Out-of-process extension surfaces" />
            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="Move one WidgetApp-owned WinUI page between an inline host slot and a host-designed popup."
                       TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Spacing="8">
            <Button x:Name="ShowInlineButton"
                    AccessKey="I"
                    AutomationProperties.Name="Show extension inline"
                    Click="ShowInlineButton_Click"
                    Content="Show inline" />
            <Button x:Name="ShowPopupButton"
                    AccessKey="O"
                    AutomationProperties.Name="Show extension in host popup"
                    Click="ShowPopupButton_Click"
                    Content="Show in host popup" />
        </StackPanel>

        <Border x:Name="InlineWidgetSlot"
                Grid.Row="2"
                MinHeight="340"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <StackPanel x:Name="InlinePlaceholder"
                        MaxWidth="480"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="8">
                <FontIcon HorizontalAlignment="Center"
                          FontSize="32"
                          Glyph="&#xE8A7;" />
                <TextBlock HorizontalAlignment="Center"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Text="Inline host slot" />
                <TextBlock HorizontalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Text="Choose either presentation. Popup chrome and placement remain owned by HostApp."
                           TextAlignment="Center"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    Spacing="8">
            <ProgressRing x:Name="ConnectionProgress"
                          Width="16"
                          Height="16"
                          IsActive="False" />
            <TextBlock x:Name="ConnectionStatus"
                       VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="The packaged extension has not been activated."
                       TextWrapping="Wrap" />
        </StackPanel>

        <Border Grid.Row="4"
                Margin="-24,0,-24,0"
                Padding="24,8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0">
            <TextBlock VerticalAlignment="Center"
                       FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="Built with love and squirrels" />
        </Border>
    </Grid>
</Window>
